# strike-it-on
Steps:
1) Clone the repository 

React app:
1) npm install to install all npm modules
2) npm start - To start the App

Node app:
1) npm install to install all npm modules
2) npm start - To start the app
